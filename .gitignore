# clasp / Apps Script local credentials & project binding — never commit
.clasp.json
.clasprc.json
apps-script/.clasp.json

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/*
!.vscode/settings.json

# node (in case tooling gets added later)
node_modules/
npm-debug.log*
